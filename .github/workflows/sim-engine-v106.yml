name: Simulation Engine v1.0.6 Legacy Regression

on:
  push:
    branches: [main]
    paths:
      - 'sim_engine/v106_*.py'
      - 'sim_engine/test_v106.py'
      - 'sim_engine/V106.md'
      - '.github/workflows/sim-engine-v106.yml'
  pull_request:
    paths:
      - 'sim_engine/v106_*.py'
      - 'sim_engine/test_v106.py'
      - 'sim_engine/V106.md'
      - '.github/workflows/sim-engine-v106.yml'

jobs:
  test:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run v1.0.6 regression tests
        working-directory: sim_engine
        run: python -m unittest -v test_v106.py
