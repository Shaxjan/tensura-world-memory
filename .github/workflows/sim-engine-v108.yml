name: Simulation Engine v1.0.8 Legacy Regression

on:
  push:
    branches: [main]
    paths:
      - 'sim_engine/v108_*.py'
      - 'sim_engine/test_v108.py'
      - 'sim_engine/V108.md'
      - '.github/workflows/sim-engine-v108.yml'
  pull_request:
    paths:
      - 'sim_engine/v108_*.py'
      - 'sim_engine/test_v108.py'
      - 'sim_engine/V108.md'
      - '.github/workflows/sim-engine-v108.yml'

jobs:
  test:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run v1.0.8 regression tests
        working-directory: sim_engine
        run: python -m unittest -v test_v108.py
